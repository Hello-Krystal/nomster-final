<!DOCTYPE html>
<html>

<head>
    <title>Nomster App</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>
    <!-- Creates a Navbar with inverse property -->
    <nav class="navbar navbar-toggleable-md navbar-inverse bg-inverse">
        <!-- This makes the name of the app a clickable buttom that brings us back to the root page -->
        <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- This links our site name to the main index page -->
        <%= link_to 'Nomster App', root_path, class: 'navbar-brand' %>

        <!-- this is a link that will perform an action -->
        <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
            <ul class="navbar-nav mr-auto mt-2 mt-md-0">
                <li class="nav-item">
                    <!-- adds plus icon to naviagation bar -->
                    <%= link_to new_place_path, class: 'nav-link' do %>
                    <!-- from gem install: font-awesome-->
                    <i class="fa fa-plus-circle"></i>
                    New Place
                    <% end %>
                </li>
            </ul>
        </div>
    </nav>

    <% if notice.present? %>
    <p class="alert alert-info">
        <%= notice %>
    </p>
    <% end %>
    <% if alert.present? %>
    <p class="alert alert-danger">
        <%= alert %>
    </p>
    <% end %>

    <%= yield %>

</body>

</html>